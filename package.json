{
  "name": "timeout-wrap",
  "version": "0.1.0",
  "description": "Wraps a Promise, resolves default value if a timeout threshold is reached",
  "repository": {
    "url": "ULIVZ/timeout-wrap",
    "type": "git"
  },
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "test:cov": "jest --coverage && npm run lint",
    "test": "npm run lint && jest",
    "lint": "xo index.js",
    "release": "release-it"
  },
  "author": "ULIVZ <chl814@foxmail.com>",
  "license": "MIT",
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {},
  "devDependencies": {
    "jest-cli": "^22.1.4",
    "eslint-config-rem": "^3.0.0",
    "xo": "^0.18.0",
    "conventional-changelog-cli": "^2.0.1",
    "release-it": "v7.4.8"
  },
  "xo": {
    "extends": "rem",
    "envs": [
      "jest"
    ],
    "rules": {
      "operator-linebreak": 0
    }
  }
}
